# Live QA Baseline - 2026-05-28

## Scope

- Baseline target: `main` after PR #75, #76, and #78.
- Local commit under test: `e79ccc9`.
- Safety boundary: no Jira writes were attempted; `DTS` was not mutated.
- Live Jira credential, JQL, and `JTFTEST` write smoke tests remain pending manual QA.

## Automated Baseline

| Check | Result | Notes |
| --- | --- | --- |
| `npm run build` | Passed | TypeScript and Vite production build completed. |
| `npm test -- --run` | Passed | 17 files, 76 frontend tests. |
| `cargo test --manifest-path src-tauri/Cargo.toml` | Passed | 148 Rust tests after cleaning stale Tauri build output. |
| `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` | Passed | Rust formatting is clean. |
| `git diff --check` | Passed | No whitespace errors in the QA branch. |

## Native QA Attempt

Command:

```bash
timeout 35s npm run tauri dev
```

Result:

- Tauri started the dev command and began compiling the Rust app.
- Vite failed before the native window opened because port `1420` was already in use.
- I did not kill the existing process because it may belong to an active user session.

Observed error:

```text
Error: Port 1420 is already in use
The "beforeDevCommand" terminated with a non-zero status code.
```

## Manual QA Still Needed

Run when port `1420` is free or the existing app session is intentionally closed:

1. Start `npm run tauri dev`.
2. Re-run the non-destructive parts of `docs/live-qa.md`: tray navigation, task focus window, JQL view, settings rendering, categories panel, backup/export dialogs if appropriate.
3. With credentials present, run read-only JQL against `DTS` and `JTFTEST`.
4. If doing write QA, use only `JTFTEST` and verify preflight, missing-description decisions, epic resolution, parent task creation, sub-task creation, and retry/recovery behavior.

## Follow-Up

- The automated baseline is mergeable evidence for the post-polish branch.
- The blocked native launch should be retried in a user-controlled desktop session before treating this as a full live QA sign-off.
